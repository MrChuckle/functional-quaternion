{
  "name": "functional-quaternion",
  "version": "0.0.1",
  "description": "A quaternion library in a functional style.",
  "scripts": {
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "build": "tsc --project tsconfig.build.json",
    "test": "vitest run",
    "dev": "vitest",
    "ci": "npm run check-format && npm run build && npm run test"
  },
  "author": "Jack Proudman",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.1.0",
    "@vitest/coverage-v8": "^3.2.4",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
